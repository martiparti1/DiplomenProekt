@model IEnumerable<GameWorld.Models.Order.OrderIndexVM>

@{
    ViewData["Title"] = "Index";
}
<style>
    #better-body{
        width: 100vh;
        margin:auto;
        right:28%;
    }
</style>
<body>
<h1>My Shopping History</h1>

<p>
    <button class="btn btn-light" onclick="location.href='@Url.Action("Index", "Product")'">Continue shopping</button>
</p>

<form asp-controller="Order" asp-action="MyOrders" method="get">
<div class="row">
  <div class="col-md-4">
    <input type="text" class="form-control rounded-pill" placeholder="Product name" aria-label="Product name" name="searchString">
  </div>
  <div class="col-md-3">
    <button class="btn btn-light" value="Filter">Filter</button>
  </div>
</div>
  
</form>
@foreach (var item in Model)
{
<section class="h-100">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-lg-10 col-xl-8">
        <div id="better-body"class="card" style="border-radius: 10px;">
          
          <div class="card-body p-4">
            <div class="d-flex justify-content-between align-items-center mb-4">
              <p class="lead fw-normal mb-0" style="color: #a8729a;">Receipt - @Html.DisplayFor(modelItem => item.OrderDate)</p>
            </div>

            <div class="card shadow-0 border mb-4">
              <div class="card-body">
                <div class="row">
                  <div class="col-md-2">
                    <img src="@Html.DisplayFor(modelItem => item.Image)"
                      class="img-fluid" alt="img"/>
                  </div>
                  <div class="col-md-2 text-center d-flex justify-content-center align-items-center">
                    <p class="text-muted mb-0">Product: @Html.DisplayFor(modelItem => item.Product)</p>
                  </div>
                  <div class="col-md-2 text-center d-flex justify-content-center align-items-center">
                    <p class="text-muted mb-0 small">Platform: @Html.DisplayFor(modelItem => item.Platform)</p>
                  </div>
                  <div class="col-md-2 text-center d-flex justify-content-center align-items-center">
                    <p class="text-muted mb-0 small">Quantity: @Html.DisplayFor(modelItem => item.Quantity)</p>
                  </div>
                  <div class="col-md-2 text-center d-flex justify-content-center align-items-center">
                    <p class="text-muted mb-0 small">Price: @Html.DisplayFor(modelItem => item.Price)</p>
                  </div>
                  <div class="col-md-2 text-center d-flex justify-content-center align-items-center">
                    <p class="text-muted mb-0 small">Discount: @Html.DisplayFor(modelItem => item.Discount)</p>
                  </div>
                  <div class="col-md-2 text-center d-flex justify-content-center align-items-center">
                    <p class="text-muted mb-0 small">Final Price: @Html.DisplayFor(modelItem => item.TotalPrice)</p>
                  </div>
                  
                
  </div></div></div></div></div></div></div></div>
</section>
}</body>